<div class="overflow-auto w-full bg-yellow-darkest border-r-2 border-black">
  <div class="float-right bg-black text-white p-3">
    <span class="uppercase text-xl custom-font">
      <%= link_to "+", new_note_path, class: "text-white no-underline hover:underline" %>
      <span class="underline">notes</span>
    </span>
  </div>
  <div class="clearfix"></div>

  <div class="mt-4 pl-2">
    <% if @notes.any? %>
      <%= render @notes %>
    <% else %>
      <div class="text-center">
        <span class="custom-font"> NOTHING FOUND! </span>
      </div>
    <% end %>
  </div>
</div>

<div class="overflow-auto w-full bg-purple-darker border-l-2 border-black">
  <div class="float-left bg-black text-white p-3">
    <span class="uppercase text-xl custom-font">
      <span class="underline">todos</span>
      <%= link_to "+", new_todo_path, class: "text-white no-underline hover:underline" %>
    </span>
  </div>
  <div class="clearfix"></div>

  <div class="mt-4 pr-2">
    <% if @todos.any? %>
      <%= render @todos %>
    <% else %>
      <div class="text-center">
        <span class="custom-font"> NOTHING FOUND! </span>
      </div>
    <% end %>
  </div>
</div>